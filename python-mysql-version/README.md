# 智能股票分析与预测系统（Python + MySQL）

西南财经大学《数据结构（课程设计）》—— 第 1-20 关实现（控制台 + Qt 图形界面）

## 技术栈

- Python 3.14+（可用 PyCharm 打开本目录）
- MySQL 8.0（数据库：`stock_information`，账号 `root` / 密码 `1433223aA!`）
- pymysql
- PySide6 6.11.2（Qt 6 GUI，已本地化到 `pylibs/`，无需系统安装）

## 项目结构

| 文件 | 说明 |
|---|---|
| `main.py` | 控制台主菜单（入口，含 1-20 关） |
| `gui.py` | **Qt 图形界面（选做）**，5 个功能标签页，覆盖 1-20 关 |
| `db.py` | MySQL 连接与数据模型（Stock / StockDetail） |
| `db_setup.py` | 建库建表 + 从 txt 导入数据（可重复执行） |
| `stock_maintain.py` | 第 1-3 关：顺序表存储 / 删除 / 修改 |
| `stock_seek.py` | 第 4-8 关：折半 / BST / 字典树 / 开放地址散列 / 链地址散列 |
| `stock_statistical.py` | 第 9-13 关：链表 / 并查集 / 堆排序 / 归并排序 / 决策树 |
| `stock_choice.py` | 第 14-17 关：BF实体识别 / BF关系抽取 / 邻接表知识图谱 / 图谱推荐 |
| `stock_prediction.py` | 第 18-20 关：皮尔逊 / 朴素贝叶斯 / KNN |
| `test_levels.py` | 核心算法单元测试（1-20 关） |
| `test_e2e.py` | 端到端回归测试（1-20 关） |
| `test_gui.py` / `test_gui_launch.py` | GUI 离屏逻辑测试 / 真实窗口启动测试 |
| `pylibs/` | 本地化 PySide6 6.11.2（Qt 6 运行库，无需 pip 安装） |
| `requirements.txt` | 依赖清单 |

## 使用步骤

### 1. 初始化数据库（首次执行）

```bash
python db_setup.py
```

数据来源：`D:\creative\Smart Stock Picking and Prediction System` 下的
`stocks.txt`、`entity.txt`、`relation.txt`、`detail/` 文件夹。
导入完成后：stocks=300, stock_daily=1500, entities=1338, relations=2700。

### 2. 运行系统

```bash
python main.py     # 控制台文本菜单
python gui.py      # Qt 图形界面 (5个标签页, 20关全部功能)
```

控制台按菜单提示输入 1-20 选择关卡，0 退出。
GUI 界面：每个功能面板输入参数后点击「执行」，结果输出到文本框（与 C 版 Qt GUI 布局一致）。

### 3. 运行测试

```bash
python test_levels.py   # 单元测试
python test_e2e.py      # 端到端回归测试
python test_gui.py      # GUI 离屏逻辑测试 (20个面板)
python test_gui_launch.py  # 真实窗口启动测试
```

## 关卡说明

### 模块一：股票信息管理与分析（第 1-13 关）

| 关卡 | 功能 | 数据结构/算法 |
|---|---|---|
| 1 | 股票信息存储：输入位置输出该位置股票全部信息 | 顺序表 |
| 2 | 股票信息删除：删除后写回 `new_stocks.txt` | 顺序表 |
| 3 | 股票信息修改：修改公司全称后写回 | 顺序表 |
| 4 | 折半查找：先按英文名二路归并排序，再折半查找，输出 ASL | 归并排序 + 折半查找 |
| 5 | 二叉排序树查找：对比原始序列与排序后重建 BST 的 ASL | BST + 快速排序 |
| 6 | 字典树前缀查找：53 子结点（26小写+26大写+空格） | Trie |
| 7 | 开放地址散列查找：线性探测，`Hash=(Σ int(cᵢ)·i²) mod 397`，表长 400 | 散列表 |
| 8 | 链地址散列查找：拉链法（头插法），输出行情明细 | 散列表 + 链 |
| 9 | 链表涨跌额分析：涨跌额<0 前、=0 中、>0 后 | 单链表 |
| 10 | 并查集行业换手率分析：按行业编码 union，汇总换手率 | 并查集 |
| 11 | 堆排序成交量 Top3：指定日期+行业，从大到小 | 堆排序 |
| 12 | 归并排序成交量统计：按日期累加汇总，从小到大 | 归并排序 |
| 13 | 决策树分类统计：二级门类→涨跌幅→成交量三条件分裂 | 决策树（二叉树存储） |

### 模块二：股票信息推荐与预测（第 14-20 关）

| 关卡 | 功能 | 数据结构/算法 |
|---|---|---|
| 14 | 基于规则的实体识别：`*` 匹配任意中文字符 | BF 暴力匹配 |
| 15 | 基于规则的关系抽取：提取两实体间内容与规则集比对 | BF 匹配 |
| 16 | 邻接表知识图谱构建：entity+relation 数据建双向图 | 邻接表（图） |
| 17 | 基于知识图谱的股票推荐：按固定顺序输出关联股票属性 | 图遍历 |
| 18 | 皮尔逊相关系数分析：5 指标与次日涨跌相关性，输出 Top3 | 相关系数 |
| 19 | 朴素贝叶斯涨跌预测（选做）：图结构存高斯参数 | 高斯朴素贝叶斯 |
| 20 | KNN 涨跌幅预测（选做）：Min-Max 归一化 + 欧氏距离 + 有序链表维护 K=3 | KNN |

## 与 C 参考实现的关系

算法逻辑与 `D:\creative\Smart Stock Picking and Prediction System`（C 语言）
参考实现一一对应：
- `stock_maintain.c` = 1-3 关
- `stock_seek.c` = 4-8 关
- `stock_statistical_analysis.c` = 9-13 关
- `stock_choice.c` = 14-17 关
- `stock_prediction.c` = 18-20 关
- `gui_qt/mainwindow.cpp` = `gui.py`（Qt 图形界面，5 个标签页布局一致）

仅数据存储从文本文件改为 MySQL（`stock_information` 库），核心数据结构
（顺序表、BST、Trie、散列表、链表、并查集、堆、归并排序、决策树、邻接表图）
均手写实现，未使用 Python 内置库替代，满足课程考核要求。

## GUI 界面说明

`gui.py` 提供 5 个功能标签页（与 C 版 Qt GUI 一致）：

| 标签页 | 功能 |
|---|---|
| ① 信息维护 (1-3关) | 第1关按位置输出、第2关删除、第3关修改 |
| ② 信息查找 (4-8关) | 折半、BST、字典树、开放地址散列、链地址散列 |
| ③ 统计分析 (9-13关) | 链表、并查集、堆排序、归并排序、决策树 |
| ④ 知识图谱 (14-17关) | 实体识别、关系抽取、图谱构建、图谱推荐 |
| ⑤ 股票预测 (18-20关) | 皮尔逊、朴素贝叶斯、KNN |

每个功能面板：输入参数（一行一个）→ 点击「执行」→ 结果输出到文本框。
数据全部来自 MySQL，GUI 启动时自动配置 PySide6 运行库路径（`pylibs/`）。
若系统已通过 pip 安装 PySide6，则优先使用系统版本。

## 注意事项

- MySQL 未启动时先启动服务 `MySQL80`（`net start MySQL80` 或服务管理器）。
- Windows 控制台中文显示：本系统已自动将 stdout 切换为 UTF-8。
- 第 2/3 关会生成 `new_stocks.txt`（UTF-8 编码，含表头）。
- 第 17 关若未先运行第 16 关，会自动构建知识图谱。
- 第 18-20 关输入支持股票代码（如 `cn_600519`）或文件路径（如 `detail/cn_600519.txt`）。
